# 办公文件排版

入口是仓库根目录的 `启动_文档格式.py`。包内模块不要单独跑。

只处理已有的 Word（.docx）和 Excel（.xlsx），不做 Markdown 转换，不写 `.bak`。跑之前先提交或另存。

请用 `--root` 指定扫描目录。Word 统一字体、行距、表格和页码。Excel 统一字体；文件名含「战斗数值框架」或「标模平衡」，或体积达到 1.5MB 的表，只改字体。
